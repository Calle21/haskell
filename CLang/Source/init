init of ({E} seq) = end = seq.end
                    if end == seq.start
                      error "init of empty list"
                      else
                      set seq.end to end - 1 \\ set is keyword ? special parse
