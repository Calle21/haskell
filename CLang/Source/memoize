memoize (R <- A f)
  mutable tree = void the Map AR
  (arg -> if res = find arg in tree
            res
            else
            res = f arg
            set tree to insert (arg,res) into tree
            res)
